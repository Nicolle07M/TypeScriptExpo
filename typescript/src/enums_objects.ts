// Enum

enum Roles {
    User = 'USER',
    Admin = 'ADMIN',
    SuperAdmin = 'SUPERADMIN'
}

console.log(Roles.Admin);


// Object

const roles = {
    User: 0,
    Admin: 1,
    SuperAdmin: 2
};

console.log(roles.User);